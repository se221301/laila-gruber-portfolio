# Laila Gruber Portfolio

Bilingual static portfolio (English default, German optional).

The language switch is in the header. The selected language is stored locally in the browser.

To publish updates:

```powershell
git add .
git commit -m "Improve language switch"
git push
```
